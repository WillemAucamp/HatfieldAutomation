# Google Sheet CSV export URL (public sheet, no auth required).
# Defaults to the Hatfield applicant sheet if left empty.
SHEET_CSV_URL=https://docs.google.com/spreadsheets/d/12uKI418JWRhns8GQpWF1ACxlKc_zN-FcXL0NC_afMZI/export?format=csv&gid=0

# Optional: path to mapping.json (defaults to ./mapping.json)
MAPPING_PATH=./mapping.json

# Run mode flags (can also pass via CLI)
DRY_RUN=false
STRICT_MODE=false
HEADLESS=false

# Process every sheet row (default). Set true / pass --skip-processed to skip already-done IDs.
SKIP_PROCESSED=false

# Leave the last applicant's browser open after the batch (headed live runs only)
KEEP_LAST_OPEN=false

# Write Status (ZAHTVW… or `error CODE`) and Timing seconds back to the Google Sheet.
# Option A: Apps Script web app URL (see apps-script/Code.gs)
SHEET_WEBHOOK_URL=
# Option B: path to a Google service account JSON (sheet must be shared with that account)
GOOGLE_SERVICE_ACCOUNT_FILE=
SHEET_ID=12uKI418JWRhns8GQpWF1ACxlKc_zN-FcXL0NC_afMZI

# Which row(s) to process (1-based, comma-separated). Empty = all unprocessed rows.
ROW_FILTER=

# Random delay range between actions (milliseconds). Default 0 for speed.
ACTION_DELAY_MIN=0
ACTION_DELAY_MAX=0

# Step screenshots (default on for dry-run only). Field read-back verify (default off).
SCREENSHOTS=false
VERIFY_FILLS=false
